import XCTest
@testable import WebTransport

final class WebTransportTests: XCTestCase {
    func testPlaceholder() throws {
        // Real tests run against the XCFramework; this just ensures
        // the test target compiles.
    }
}
